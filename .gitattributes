# Receipt fixture hashes cover complete file bytes, so checkout newlines are contractual.
tests/fixtures/pulse/happy-path/evidence/receipts/*.json text eol=lf
tests/fixtures/pulse/lifecycle-history/packages/*/evidence/receipts/*.json text eol=lf

# Committed Pulse public projections are compared byte-for-byte with deterministic LF output.
src/data/pulse/public-*.json text eol=lf

# The canon relationship projection is also verified byte-for-byte.
src/data/projectRelationships.json text eol=lf

# Shared claim projections have the same deterministic byte contract.
src/data/project-claims.json text eol=lf
src/data/claim-consumers.json text eol=lf
