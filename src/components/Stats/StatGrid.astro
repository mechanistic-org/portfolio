---
interface Props {
	cols?: 2 | 3 | 4;
}

const { cols = 3 } = Astro.props;
const colClass = { 2: "md:grid-cols-2", 3: "md:grid-cols-3", 4: "md:grid-cols-4" }[cols];
---

<div class={`not-content my-8 grid grid-cols-2 gap-3 ${colClass}`}>
	<slot />
</div>
