---
import CareerTimeline from "../DataViz/CareerTimeline";
import type { CareerNode } from "../../utils/contextRibbon";
interface Props {
	nodes: CareerNode[];
	currentId: string;
}
const { nodes, currentId } = Astro.props;
---

<CareerTimeline nodes={nodes} currentId={currentId} />
